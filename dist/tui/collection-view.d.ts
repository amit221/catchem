import React from "react";
import { GameState } from "../core/types";
interface CollectionViewProps {
    state: GameState;
}
export declare function CollectionView({ state }: CollectionViewProps): React.ReactElement;
export {};
